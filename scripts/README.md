# Scripts

| File | Role |
| --- | --- |
| `seed.py` | Deterministic seeds for every run |
| `check_env.py` | Verify torch / transformers / transformer_lens |
| `signals.py` | Log **loss + geometry** side by side to `results/` |

Experiment entrypoints (ROME baseline, geometry-aware edits, plots) go here next.
