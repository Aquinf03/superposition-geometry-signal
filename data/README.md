# Data

Fact triples and probe sets for track / diff / edit evals.

| File | Role |
| --- | --- |
| `eiffel_rome.yaml` | Demo factual edit (Eiffel Tower: Paris → Rome) |

Keep large raw dumps out of git (`data/raw/`, `data/processed/` are gitignored).
